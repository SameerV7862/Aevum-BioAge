{
  "version": 2,
  "buildCommand": "cd apps/mobile && flutter pub get && flutter build web --release",
  "outputDirectory": "apps/mobile/build/web",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
